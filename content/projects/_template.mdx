---
# Copy this file to `content/projects/your-project-slug.mdx` and fill it in.
# The filename becomes the URL: /projects/your-project-slug
#
# Files starting with "_" are ignored by the loader, so this template never
# shows up on the site and you can use the same trick for drafts.

title: "Project Name"
summary: "One or two sentences a recruiter can skim. What it does and why it is interesting — not how it is built."
stack: ["TypeScript", "PostgreSQL"]
repo: "https://github.com/Yaswanth6303/your-repo" # optional, delete if private
demo: "https://example.com" # optional, delete if there is nothing live
image: "https://images.unsplash.com/photo-xxxxxxxx?w=1200&q=80"
date: "January 1, 2026" # sorts the grid, newest first
period: "2026" # optional, shown on the detail page
role: "Solo project" # optional
status: "shipped" # shipped | in-progress | archived
featured: false # featured projects appear on the homepage
---

Open with the problem, in plain language. What was broken or missing, and who cared? Two or three sentences is plenty — this is the part people actually read.

## What it does

The capabilities, briefly. Bullets are fine here.

## How it works

The architecture and the reasoning behind it. This is where you show engineering judgment, so favour decisions over feature lists: what you chose, what you rejected, and why.

```ts
// A short, representative snippet beats a long one.
export function example() {
  return "syntax highlighting works here"
}
```

## Challenges

The hardest problem you hit and how you solved it. Interviewers ask about this more than anything else on the page.

## What I would change

Honest limitations and what you would do with more time. This reads as confidence, not weakness.
